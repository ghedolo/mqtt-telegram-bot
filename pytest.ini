[pytest]
# tools/ is on the path so the maintenance scripts (split_device, rename_device,
# cadence, ...) stay importable by their bare module name, from tests and from
# each other
pythonpath = . tools
testpaths = tests
addopts = -q
