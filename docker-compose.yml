services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./sensors.yaml:/app/sensors.yaml:ro
      - ./credentials.yaml:/app/credentials.yaml:ro
    environment:
      - TZ=Europe/Rome
